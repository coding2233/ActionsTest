name: CI-Activation

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        lfs: true
    - name: test
      run: |
        #pip3 install protobuf
        echo "hello "
