name: CI-Activation

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        lfs: true
    - name: test
      run: |
        apt install -y python3-protobuf
        echo "hello "
